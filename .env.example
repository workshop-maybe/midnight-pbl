# Andamio API key — required, never exposed to client.
ANDAMIO_API_KEY=

# Network profile selector — "preprod" or "mainnet".
# Everything else (gateway URL, course ID, access token policy ID) is
# resolved from src/config/networks.ts at build time.
PUBLIC_ANDAMIO_NETWORK=preprod
